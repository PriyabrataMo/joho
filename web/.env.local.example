# Copy to .env.local for local dev. In production (Vercel), set this in the project's
# Environment Variables to the deployed gateway URL (e.g. the Cloud Run URL).
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8000
